# Dependencies
node_modules/

# Secrets - NEVER commit real .env files
.env
.env.local
.env.*.local

# Databases, backups and local scratch state (dev SQLite lives here)
data/
*.db

# Uploads (student photos, madrasa logos)
uploads/

# Logs
*.log
logs/

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
.dev-credentials.txt
.devdata/

# Promo pipeline render outputs (regenerate via tools/promo scripts)
tools/promo/dist/

# Agent workspace scratch (dev-only preview/seed scripts, never shipped)
.arena/
